{% extends 'base1.html.twig' %}

{% block title %}Edit Offre{% endblock %}

{% block body %}
    <h1>Edit Offre</h1>

    {{ include('offre1/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_offre1_index') }}">back to list</a>

    {{ include('offre1/_delete_form.html.twig') }}
{% endblock %}
