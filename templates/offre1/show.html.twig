{% extends 'base1.html.twig' %}

{% block title %}Offre{% endblock %}

{% block body %}
    <h1>Offre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ offre.date ? offre.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ offre.prix }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ offre.image }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ offre.dateFin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_offre1_index') }}">back to list</a>

    <a href="{{ path('app_offre1_edit', {'id': offre.id}) }}">edit</a>

    {{ include('offre1/_delete_form.html.twig') }}
{% endblock %}
